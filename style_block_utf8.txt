<style>
        #hub-bubble { z-index: 5 !important; top: -15px !important; }
    </style>

<style>
        #modulo-feedbacks, #modulo-grupos {
            width: 100vw !important;
            position: relative;
            left: 50%;
            transform: translateX(-50%);
            max-width: 100vw !important;
            border-radius: 0 !important;
            border-left: none !important;
            border-right: none !important;
            padding: 25px 5vw !important; /* Add some padding so the content doesn't touch the edges */
        }
    </style>

<style>#modulo-estrelas { width: 100%; } .admin-table-wrapper { padding: 0; }select option { color: #000; background: #fff; }
            </style>

<style>
                .form-tab-content { display: none; animation: fadeIn 0.4s ease forwards; }
                .form-tab-content.active { display: block; }
                @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
                select option { color: #000; background: #fff; }
            </style>